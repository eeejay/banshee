#!/bin/bash

if [ -e ./build-clean ]; then
	POP=yes
	pushd ../..
fi

for file in `find .`; do
	if [ `echo "$file" | grep .svn` ]; then
		continue
	fi

	if [ -d "$file" ]; then
		echo $file
		rm -f "$file/*.{mdb,dll,exe}"
	fi
done

