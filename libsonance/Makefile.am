INCLUDES = $(SONANCE_CFLAGS)

sonancelibdir = $(pkglibdir)
sonancelib_LTLIBRARIES = libsonance.la

libsonance_la_SOURCES = \
	eggtrayicon.c eggtrayicon.h

libsonance_la_LIBADD = $(SONANCE_LIBS)

DISTCLEANFILES = Makefile.in

