ASSEMBLY = Banshee.Widgets
TARGET = library
ASSEMBLY_BUILD_FLAGS = -unsafe
LINK = $(REF_BANSHEE_WIDGETS)
SOURCES = Banshee.Widgets/*.cs
RESOURCES = Resources/*

include $(top_srcdir)/build/build.mk

EXTRA_DIST += $(ASSEMBLY_FILE).config.in

